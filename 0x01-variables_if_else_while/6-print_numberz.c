#include <stdio.h>

/**
 * main - this prints all single digit numbers of base 10
 * Return: 0 on success
 */
int main(void)
{
	int z;

	for (z = 48; z < 58; z++)
	{
		putchar(z);
	}
	putchar('\n');
	return (0);
}
