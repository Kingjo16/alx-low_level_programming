#include <stdio.h>

/**
 * main - this prints all single dgit number followed ny 10 string
 * Return: 0 on succesful return
 */
int main(void)
{
	int z;

	for (z = 0; z < 10; z++)
		printf("%d", z);
	printf("\n");
	return (0);
}
